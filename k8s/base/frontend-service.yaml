apiVersion: v1
kind: Service
metadata:
  name: pci-frontend
  labels:
    app: pci-frontend
    component: frontend
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 3000
    protocol: TCP
    name: http
  selector:
    app: pci-frontend
    component: frontend
