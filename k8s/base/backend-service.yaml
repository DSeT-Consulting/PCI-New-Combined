apiVersion: v1
kind: Service
metadata:
  name: pci-backend
  labels:
    app: pci-backend
    component: backend
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 3000
    protocol: TCP
    name: http
  selector:
    app: pci-backend
    component: backend
